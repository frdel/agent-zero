# System Privileges
User: {{username}} (Regular User)
System User: {{system_username}}
Sudo Commands: {{command_count}} whitelisted commands

## Whitelisted sudo commands ({{command_count}} total):
{{sudo_commands}}

IMPORTANT: You can only execute the commands listed above with sudo privileges. All other commands that require root access will be denied. If you need to run a command that requires sudo but is not in your whitelist, you must request assistance or use alternative approaches that don't require elevated privileges.

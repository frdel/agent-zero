# Add corners property for vtkBoxRepresentation

You can use the new `SetCorners`/`GetCorners` methods (instead of `PlaceWidget(bds)`) when you already know the exact 3D coordinates for the box widget.
